package com.itsm.ticketing.entity;

/**
 * Enum representing the roles a user can have in the ITSM system.
 * ADMIN - Full access to all system features
 * USER  - Limited access (can create and view tickets)
 */
public enum Role {
    ADMIN,
    USER
}
